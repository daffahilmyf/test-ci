pipeline {
    agent any

    stages {
        stage("Verify cargo") {
            
            echo "Verify cargo"
        }
        stage("Build") {
            echo "Build"
        }
        stage("Test") {
            echo "Test"
        }
    }
}
